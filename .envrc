nvmrc=~/.nvm/nvm.sh
if [ -e $nvmrc ]; then
  source $nvmrc
  nvm use
fi

PATH_add node_modules/.bin
if [ -x "$(command -v cowsay)" ] && [ -x "$(which lolcat)" ]; then
  git log -1 | cowsay -f dragon-and-cow | lolcat
fi
